/* When concatenating with the + operator, if one operand is a string, 
JavaScript will coerce the non-string operand into a string as well. The result
is another string. This is implicit type coercion. */